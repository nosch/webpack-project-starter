'use strict';

var React = require('react');

var AppContent = React.createClass({
    render: function () {
        return (
            <h1>This is the APPLICATION content.</h1>
        );
    }
});

module.exports = AppContent;
